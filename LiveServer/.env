PORT = 8000
# Server Configuration
PORT=3000                    # The port your server will listen on
PUBLIC_IP=13.123.45.67      # Your AWS instance's public IP address
NODE_ENV=production         # Environment: 'development' or 'production'

# Security
ALLOWED_ORIGINS=https://your-frontend.com,https://app.your-frontend.com    # Comma-separated list of allowed frontend origins

# Performance
WORKER_POOL_SIZE=4          # Number of Mediasoup workers (recommend: number of CPU cores)
# If using Elastic IP
PUBLIC_IP=your-elastic-ip

# If using EC2 public IP
PUBLIC_IP=your-ec2-public-ip
NODE_ENV=production
ALLOWED_ORIGINS=https://your-production-domain.com

NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173